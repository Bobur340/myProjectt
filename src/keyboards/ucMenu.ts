import { InlineKeyboard } from "grammy";

export const ucMenu = new InlineKeyboard()
  .text("60 UC — 8000", "uc_60").row()
  .text("325 UC — 32000", "uc_325").row()
  .text("825 UC — 70000", "uc_825");
